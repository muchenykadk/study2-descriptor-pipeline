# Paper §4 — Study 2: Fragment Descriptor Pipeline (draft)
*Working draft v2, 2026-07-23. Compact four-phase version (Muchen's structure) with corrections folded in: input asymmetry, UNSCANNED method, design-factor status honesty, ten real fragments, JSON-record precision. Target ~2–2.5 pp incl. Fig. 2. No em-dashes; no "rather than" constructions.*

---

## Study 2: Fragment Descriptor Pipeline

Study 2 responds to the gaps identified in Study 1 by developing a prototype computational pipeline that takes a 3D scan of an irregular concrete fragment, enriches it with extracted geometric and surface descriptors and their procedural and design implications, and organizes the result into a queryable record with an interactive 3D viewer. Its aim is to render the manual observations of Study 1 computable, preparing per-fragment records in a queryable form so that multi-criteria assessment can scale beyond manual curation. Fig. 2 traces the pipeline dataflow, from scan input through parallel geometric and surface characterization to the consolidated fragment record.

The pipeline accepts two scan input types. A colour point cloud (PLY) from LiDAR-based capture, cleaned and downsampled in CloudCompare, provides the geometric descriptors; a photogrammetric textured mesh, prepared in Blender through remeshing, UV unwrapping, and texture rebaking, additionally supports surface classification and its spatial localization. Because a fragment is scanned resting on the ground, its contact face is never captured, and reconstructing a closed mesh manufactures surface where none was observed. This contact region is marked during preparation so that, downstream, planes matching its orientation are flagged as unreliable and the region is masked out of surface classification, keeping observed surface distinct from reconstructed filler.

From the prepared scan, the pipeline extracts two descriptor classes in parallel. Geometric descriptors are computed deterministically: an oriented bounding box gives principal dimensions and volume, with a convexity ratio and an estimated mass; RANSAC segmentation identifies planar regions with their area, orientation, and fit deviation; and multi-scale curvature captures both overall form and fine surface texture. Surface descriptors are obtained from the texture: a vision-language model (GPT-4o) classifies surface conditions against a controlled seven-class taxonomy with multi-run majority voting, and an 8x8 grid over the texture localizes each label and reprojects it onto the mesh through shared UV coordinates.

The record also carries a layer of procedural, action-oriented knowledge, encoded as design factors that link surface and geometry conditions to their implications for cutting, connection feasibility, stacking stability, and finishing. At this stage this layer is a placeholder that demonstrates the schema's linkage structure, not a validated knowledge base. Its entries are drawn provisionally from the on-site experience of Study 1 and the concrete-reuse and fabrication literature; the rules that would derive conclusions such as connection strategy or handling class from the descriptors are specified but not yet executed; and grounding the mappings in verified domain expertise remains future work.

All descriptors, together with registry provenance data such as source-building metadata, are aggregated into a fragment-level JSON record conforming to the schema, each field carrying its computation method and data status, and rendered in an interactive HTML report with a 3D viewer and per-feature spatial overlays. The pipeline is demonstrated on ten real fragment scans from Study 1, confirming technical feasibility of the end-to-end workflow and positioning the instrument as the material audit layer of the broader feature-aware reuse framework.

---

## Notes / flags

- **v2 = compact version (2026-07-23)**: replaced the expanded bold-header draft with Muchen's compact four-phase prose + the five corrections. Content held in reserve from the expanded draft, fold into §4.4 if more depth wanted: (a) open-vocabulary capability vs. controlled-deployment clarification (pre-empts "why only seven classes?"); (b) per-face binding honesty — Phase 3B localization currently supports visualization/inspection, but binding dominant labels to individual planar regions (face-to-label join) does not yet exist and is what would make surface condition queryable per face.
- **Fragment count = ten** (Muchen): confirm all ten are registered in fragments_manifest.csv and processed through the pipeline before submission (repo previously showed 5 registered / 2 complete). If some scanned-but-not-processed by August, say "scanned" vs "processed" precisely.
- **Phases REMOVED 2026-07-23** (Muchen): dropped the four-phase numbering. It misordered the workflow (mesh reconstruction happens in scan prep, up front, not after geometry) and mismatched Fig. 2 (scan prep → parallel geometric + surface extraction → consolidation, not linear phases). Text now: inputs + prep (UNSCANNED marked at prep), then two parallel descriptor streams, then domain-knowledge + aggregation. Matches the diagram.
- **"surface roughness" resolved 2026-07-23**: replaced the standalone-roughness claim with "multi-scale curvature captures both overall form and fine surface texture" (accurate to geometry.py, where fine-scale curvature is the roughness proxy). No separate roughness descriptor claimed.
- **UNSCANNED**: now stated at scan prep (marking) with downstream application (flag planes / mask texture). Outline §4.3 wanted it given space as the novel detail; if budget allows, expand with sidecar/threshold specifics (25° normal test) + small figure (FS-006 grid with cleared cells). Closing "distinct from reconstructed filler" plants the §9 outlook hook.
- **Data-status honesty**: aggregation says each field carries computation method + data status. Pseudo entries for unmeasured attributes (concrete class, rebar) + provisional design-factor knowledge = the three-tier accounting; full statement lives in §5 Results. Keep consistent.
- **Reserve content** (fold in if depth wanted): open-vocabulary capability vs. controlled-deployment clarification (pre-empts "why only seven classes?"); per-face binding honesty (spatial localization currently supports visualization; the face-to-label join that would make surface condition queryable per face does not yet exist).
- **Fig. 2 caption** (Muchen): "Study 2: Fragment Descriptor Pipeline - from 3D Scan to Enriched Queryable Database." Consider "Record" over "Database" (no multi-fragment database demonstrated yet). Fix figure typo "Querable" → "Queryable".
- **Figure vs. text asymmetry (OPEN)**: diagram shows Cleaned PointCloud → Open3D rendered images → Feature Classification, implying the pcd path reaches classification and lacks only Spatialization. Text says pcd path is geometry-only (per HANDOVER). Confirm whether rendered-image classification is implemented or planned; then soften text or dash the figure arrow as planned.
- Terminology: "descriptors" for extracted values; "design factors" only for the linkage layer; "registry data" for provenance/source metadata.
