numpy
scipy
trimesh
open3d
Pillow
fast-simplification
jsonschema
python-dotenv

# Region classification, the default surface path, is implemented for OpenAI only.
openai

# Used by the legacy grid path (--grid-legacy) for its other providers.
anthropic
requests
